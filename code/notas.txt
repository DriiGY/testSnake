bool32 MessageResult = GetMessageA(&Message, Window, 0, 0);
                if(MessageResult>0)
                {
                    
                    TranslateMessage(&Message); 
                    DispatchMessage(&Message);
                }
                else
                {
                    GlobalRunning = false;
                }